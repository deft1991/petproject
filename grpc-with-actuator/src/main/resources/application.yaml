server:
  port: 8081

spring:
  application:
    name: grpc-with-actuator
  boot:
    admin:
      client:
        url: http://localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: '*'
    health:
      show-details: always

